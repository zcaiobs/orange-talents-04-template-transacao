spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/apidatabase?createDatabaseIfNotExist=true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: off

server:
  error:
    include-stacktrace: never
